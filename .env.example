# Fork Test Configuration
BASE_RPC_URL=https://mainnet.base.org

# Deployment Configuration
# Copy this file to .env and fill in the values

RPC_URL=

# Deployer Configuration
# Option 1: Use a specific address (recommended for production)
DEPLOYER_ADDRESS=

# Option 2: Use a mnemonic (for testing)
MNEMONIC=

# Vault Creation (Optional - only needed if CREATE_VAULT=true)
CREATE_VAULT=false              # Set to "true" to create a vault during deployment

VAULT_OWNER=                    # Owner of the vault (defaults to deployer)
TIMELOCK=                       # Initial timelock in seconds (default: 7 days = 604800)
ASSET=                          # Address of the underlying asset (e.g., USDC)
VAULT_NAME=                     # Name of the vault (e.g., "Ample USDC")
VAULT_SYMBOL=                   # Symbol of the vault (e.g., "aUSDC")

# Note: Chain-specific configs (EVC, Permit2, Perspective, VRF Coordinator, etc.)
# are automatically loaded from script/ample/config/*.sol based on the network you deploy to.
# Update those files if you need to change chain-specific addresses.
